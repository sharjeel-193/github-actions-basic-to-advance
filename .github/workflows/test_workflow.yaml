name: Testing of Composite Action Workflow

on:
  push:
    branches:
      - main

jobs:
    run-tests-for-composite-action:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout code
              uses: actions/checkout@v2

            - name: Run Composite Action developed by Saptarshi
              uses: Saptarshi666/github-actions-basic-to-advance/.github/actions/composite@main

            - name: Run Composite Action developed by Gemma
              uses: bgerelmaa/github-actions-basic-to-advance/.github/actions/composite@main

            - name: Run Composite Action developed by Kourosh
              uses: kooroshsajadi/github-actions-basic-to-advance/.github/actions/composite@main
              
              
